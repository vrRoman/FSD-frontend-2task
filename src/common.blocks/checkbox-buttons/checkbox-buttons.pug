include __button/checkbox-buttons__button


mixin checkbox-buttons({items, isRich = false})
    -
        let className = 'checkbox-buttons';
        if (isRich) {
            className += ' checkbox-buttons_rich';
        }
    div(class=className)
        each item in items
            +checkbox-buttons__button({
              item,
              isRich
            })
