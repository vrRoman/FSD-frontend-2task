include ../heading/heading


mixin elem-info(title, state, isShort = false)
    .elem-info(class=isShort ? 'elem-info_short' : false)
        .elem-info__title
            +heading({text: title})
        if (state)
            .elem-info__state= state
