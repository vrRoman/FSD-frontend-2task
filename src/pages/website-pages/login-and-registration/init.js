/* eslint-disable no-unused-vars */

import LoginAndRegistration from '@/pages/website-pages/login-and-registration/LoginAndRegistration';

$('.js-login-and-registration').each(function init() {
  const loginAndRegistration = new LoginAndRegistration(this);
});
